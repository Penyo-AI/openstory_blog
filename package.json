{
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.17"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:sitemap": "node scripts/generate-sitemap.cjs",
    "prebuild": "npm run docs:sitemap",
    "build": "vitepress build",
    "docs:build": "npm run build",
    "docs:preview": "vitepress preview"
  }
}
